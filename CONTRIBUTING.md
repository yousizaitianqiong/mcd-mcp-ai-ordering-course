# 小组协作说明

本仓库用于问题求解实战课程四人小组项目“AI 麦乐送助手”的共同开发。公开仓库只提交项目源码、通用文档和不含个人信息的配置示例；教师提交版 Word 文档、成员学号、真实测试地址和本地凭据保留在仓库之外。

## 分支与合并

- main 用于稳定版本和课堂演示基线。
- 每项工作从最新 main 创建独立分支。
- 功能分支使用 feat/功能名，问题修复使用 fix/问题名，文档修改使用 docs/主题。
- 完成后提交 Pull Request，描述改动、验证命令、证据位置和已知限制，再合并到 main。
- 不直接在 main 上提交未验证的大块改动。

## 四人任务方向

1. 需求与文档：用户故事、需求追踪、架构接口、GitHub 调研矩阵、课堂演示、最终报告和迭代记录。
2. MCP 与模型后端：远程 MCP 客户端、Provider、模型适配、工具白名单和人工确认闸门。
3. Web 前端：聊天窗口、SSE 事件、工具进度、地址/门店、菜单、购物车、核价确认和订单状态页面。
4. Mock、测试与集成：模拟数据、手工验收、构建验证、异常场景、真实联调协调和脱敏证据。

成员使用 A-D 占位；不要把真实姓名、学号、联系方式或个人地址写入公开仓库。

## Issue、状态与证据

- 每项工作先建立或认领 Issue，在 Issue 或 Pull Request 中记录目标、负责人和验收条件。
- 文档、Mock、本地构建和真实 MCP 结果分别记录，不能用 Mock 结果替代真实联调证据。
- 迭代状态统一使用“计划中、进行中、已验证、阻塞”。
- 真实联调缺少 Token、在线模型、测试账号、配送地址或网络时，标记为阻塞/待凭据，并写明解除条件。
- 合并前至少运行 pnpm test 和 pnpm build；必须记录 pnpm test 实际发现的测试数量。当前没有自动化测试文件时，不得写成测试全部通过。

## 真实下单规则

- Issue #1 的真实联调必须先完成地址、门店、菜单、优惠券和核价查询，再由两名成员复核后创建一次真实待支付订单。
- 一名操作成员点击确认，另一名成员复核地址、商品、金额和报价有效期。
- 不自动支付；Provider 失败或返回未知状态时不自动重试 create-order。
- 真实订单、测试账号、服务条款和外部副作用必须在仓库外确认。
- 真实订单状态、时间和标识写入测试记录时必须脱敏；支付凭据不得保存。

## 安全规则

- 严禁提交 .env、MCP Token、模型 API Key、Authorization Header 或真实支付信息。
- 不提交真实配送地址、手机号、成员学号和教师提交材料。
- 不把 Token 放入前端代码、URL、日志、截图或报告附件。
- data/app-state.json 只用于本地演示，并保持在 .gitignore 覆盖范围内。
- create-order 相关改动必须说明人工确认、报价有效期、重复提交控制和是否触发外部副作用。

## 交付文档

- docs/requirements.md：编号化需求、用户故事和验收标准
- docs/architecture.md：架构、领域接口、HTTP API、SSE 事件和安全边界
- docs/research-matrix.md：GitHub 调研、融合边界和许可证核查记录
- docs/demo-script.md：Mock 主流程和真实 MCP 受控演示
- docs/test-plan.md：命令、Mock、MCP 和安全测试计划
- docs/team-roles.md：A-D 分工、排期和真实下单复核规则
- docs/final-report.md：最终报告结构和状态填写规则
- docs/iteration-log.md：迭代目标、证据、状态和风险
