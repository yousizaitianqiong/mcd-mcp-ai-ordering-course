{
  "name": "mcd-mcp-ai-ordering-course",
  "private": true,
  "version": "0.1.0",
  "description": "问题求解实战课程项目：AI 麦乐送助手",
  "scripts": {
    "dev": "pnpm --parallel --filter @mcd/server --filter @mcd/web dev",
    "dev:server": "pnpm --filter @mcd/server dev",
    "dev:web": "pnpm --filter @mcd/web dev",
    "build": "pnpm -r build",
    "test": "pnpm --filter @mcd/server test",
    "test:mock": "pnpm --filter @mcd/server exec tsx --test src/http.e2e.test.ts",
    "start": "pnpm --filter @mcd/server start"
  }
}
