# 小组协作说明

本仓库用于问题求解实战课程四人小组项目“AI 麦乐送助手”的共同开发。公开仓库只提交项目源码、通用文档和不含个人信息的配置示例；教师提交版 Word 文档、成员学号和本地凭据保留在仓库之外。

## 分支与合并

- `main` 用于稳定版本和课堂演示基线。
- 每项工作从最新 `main` 创建独立分支。
- 功能分支使用 `feat/功能名`，问题修复使用 `fix/问题名`，文档修改使用 `docs/主题`。
- 完成后提交 Pull Request，描述改动、验证命令和已知限制，再合并到 `main`。
- 不直接在 `main` 上提交未验证的大块改动。

## 四人任务方向

1. 需求与文档：需求编号、架构说明、调研矩阵、演示脚本和最终报告。
2. MCP 与模型后端：远程 MCP 客户端、Provider、模型适配、工具白名单和确认闸门。
3. Web 前端：聊天窗口、工具进度、菜单、购物车、核价确认和订单状态页面。
4. Mock、测试与集成：模拟数据、异常测试、端到端验证和课堂部署说明。

## Issue 与进度

- 每项工作先建立或认领 Issue，在 Issue 中记录目标、负责人和验收条件。
- 开发过程中在 Issue 或 Pull Request 中更新进度和测试结果。
- 合并前至少运行 `pnpm test` 和 `pnpm build`；如果外部 MCP 或在线模型不可用，必须注明使用 Mock 模式验证。

## 安全规则

- 严禁提交 `.env`、MCP Token、模型 API Key、Authorization Header 或真实支付信息。
- 不提交真实配送地址、手机号、成员学号和教师提交材料。
- 不把 Token 放入前端代码、URL、日志或截图。
- `create-order` 相关改动必须说明人工确认、报价有效期和重复提交控制。
